# Source of truth. Compile to requirements.txt WITH HASHES:
#   pip install pip-tools
#   pip-compile --generate-hashes requirements.in -o requirements.txt
# Commit both files. The lock file is what makes the build reproducible.
# In this case I use those command instead because my WSL could not use pip install pip-tools
# pip install uv
# uv pip compile --generate-hashes requirements.in -o requirements.txt 
scikit-learn==1.8.0
scipy==1.16.3
pandas==3.0.2
numpy==2.4.4
mlflow==3.16.0
pytest==9.1.1
PyYAML==6.0.3
joblib==1.4.2
ruff==0.16.6
fastapi==0.141.1
httpx2==2.12.0
